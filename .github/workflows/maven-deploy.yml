name: Maven - CI

on:
  push:
    branches: [ main ]
  pull-request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:

    - name: Setup Maven
      uses: s4u/setup-maven-action@v1.14.0

    - name: Build
      run: mvn --batch-mode verify
